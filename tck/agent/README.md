# Sample Agent

This agent is a sample to emulate the task flow in a streaming scenario, and it will be tested against the tck tests in the CI. To run:

```bash
npm run tck:sample-agent
```

The agent will start on `http://localhost:41241`.
