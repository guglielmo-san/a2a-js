{
  "name": "samples",
  "version": "1.0.0",
  "description": "Samples for A2A-js protocol",
  "type": "module",
  "scripts": {
    "build:samples": "tsup agents/**/index.ts extensions/index.ts --out-dir dist/samples --dts --format esm",
    "type-check": "tsc --noEmit --project tsconfig.check.json",
    "a2a:cli": "tsx cli.ts",
    "agents:movie-agent": "tsx agents/movie-agent/index.ts",
    "agents:sample-agent": "tsx agents/sample-agent/index.ts",
    "agents:extension-agent": "tsx extensions/index.ts"
  },
  "devDependencies": {
    "@genkit-ai/googleai": "^1.24.0",
    "@types/express": "^5.0.5",
    "express": "^5.1.0",
    "genkit": "^1.24.0"
  }
}
